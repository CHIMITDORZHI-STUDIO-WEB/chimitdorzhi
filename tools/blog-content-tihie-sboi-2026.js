// Контент статьи: тихие сбои — баг, который прошёл все тесты
module.exports = `
<div class="blog-tldr">
  <h3>Коротко (TL;DR)</h3>
  <ul>
    <li>Бот работал, сервис был «зелёный», логи чистые — и при этом ни одна кнопка не работала. Ни у кого. Никогда.</li>
    <li>Причина — цепочка проверок в неправильном порядке. Каждое поле в ней реально существует, дефолт разумный, на ревью читается корректно.</li>
    <li>Юнит-тесты проходили, потому что подавали синтетическое событие, в котором нужного поля просто нет. Тест защищал от случая, которого не бывает.</li>
    <li>Это целый класс дефектов: правильный и неправильный ответ имеют одинаковую форму, одинаковый поток управления и одинаковые логи.</li>
    <li>Генерация кода моделями делает такие дефекты массовыми: модель отлично пишет правдоподобный код и ничего не знает про поведение конкретной платформы.</li>
    <li>Лечится не количеством тестов, а четырьмя приёмами: реальный трафик вместо фикстур, дифф выхода на одинаковом входе, проверки на эффект вместо отсутствия ошибки, явный ответ на вопрос «что будет на шаге, который система не тянет».</li>
  </ul>
</div>

<h2 id="scena">Сцена: всё работает, ничего не работает</h2>

<p>Я веду один и тот же продукт в двух мессенджерах — в Telegram и в MAX. Внешне платформы похожи настолько, что это усыпляет. Бот отправляет сообщение с инлайн-кнопками, человек жмёт кнопку, обработчик находит пользователя и делает своё дело. Логика на обеих платформах одна и та же, различаться должны только детали транспорта.</p>

<p>Мы выкатили версию для MAX. Сервис поднялся, вебхук отвечал 200, ошибок в мониторинге ноль, в логах — ровно то, что ожидалось от здорового сервиса. И при этом ни одна кнопка не работала. Не «иногда», не «у части пользователей», не «под нагрузкой». Совсем. У всех. С первой минуты.</p>

<p>Самое неприятное в этой истории даже не то, что баг был. Баг был бы полбеды, если бы он как-то о себе заявил. Проблема в том, что система не имела ни одного способа сообщить о нём. Ни исключения, ни предупреждения, ни аномалии в метриках. Формально всё было в порядке — и формально всё оставалось в порядке ровно столько, сколько мы не смотрели на продукт глазами пользователя.</p>

<h2 id="prichina">Причина: цепочка в неправильном порядке</h2>

<p>Разница между платформами оказалась в том, кого считать отправителем события.</p>

<p>В Telegram, когда человек нажимает инлайн-кнопку, в апдейте приходит именно тот, кто нажал. Читаешь пользователя из события — и дальше работаешь с ним.</p>

<p>В MAX нажатие кнопки приходит как событие, которое содержит <strong>сразу два объекта</strong>: <code>callback</code> и то самое <code>message</code>, к которому кнопка была прикреплена. И вот тут ловушка: <code>message.sender</code> — это автор сообщения. А автор сообщения с кнопками — сам бот. Человек, который нажал, лежит отдельно, в <code>callback.user</code>.</p>

<p>Код выглядел так:</p>

<pre><code>sender = (
    message.get("sender")      # бот — на каждом нажатии
    or callback.get("user")    # сюда никогда не доходило
    or payload.get("user")
    or {}
)</code></pre>

<p>Посмотрите на эту конструкцию внимательно. Она не выглядит ошибочной. Все три поля существуют в документации платформы. Есть аккуратный дефолт — пустой словарь, никакого падения по <code>None</code>. Порядок «сначала отправитель сообщения, потом колбэк» кажется естественным: сообщение — более общий случай, колбэк — частный.</p>

<p>Но событие нажатия кнопки <strong>всегда</strong> несёт в себе сообщение. Значит первая ветка срабатывала всегда. Значит <code>sender</code> на каждом нажатии становился ботом.</p>

<p>Дальше код брал идентификатор из этого объекта, шёл в базу искать пользователя, не находил его — потому что бот не зарегистрирован как пользователь — и возвращался. По штатной ветке. Без исключения. Ничего не сломалось: просто ответ на вопрос «кто нажал» оказался неверным, а неверный ответ имел ровно ту же форму, что и верный.</p>

<div class="blog-callout blog-callout-info"><i class="ph-fill ph-info" aria-hidden="true"></i> Исправление — одна переставленная строка: <code>callback.get("user")</code> поднимается на первое место. В этом и суть проблемы: починка тривиальная, а вот обнаружение — нет.</div>

<h2 id="pochemu-ne-poymali">Почему это не поймал ни один рубеж защиты</h2>

<p>Разберём по слоям, потому что каждый слой промахнулся по своей собственной причине, и эти причины стоит понимать отдельно.</p>

<p><strong>Компилятор и типы.</strong> Претензий нет и быть не могло. Типы сходятся: словарь, метод <code>get</code>, строка на выходе. Все ключи, к которым мы обращаемся, действительно есть в полезной нагрузке платформы. С точки зрения статического анализа перед нами абсолютно здоровый код.</p>

<p><strong>Рантайм.</strong> Вебхук вернул 200. Никакой обработчик исключений не сработал, потому что исключений не было. Health-check зелёный, аптайм сто процентов. Мониторинг честно показывал, что сервис жив — он и был жив.</p>

<p><strong>Логи.</strong> Логировать было нечего. Ситуация «искали пользователя, не нашли» — это законный результат поиска, а не сбой. Если писать в лог каждый ненайденный объект, лог превратится в шум за сутки. Успех и этот конкретный провал шли по одному и тому же пути в коде, а логи описывают путь, а не смысл.</p>

<p><strong>Юнит-тесты.</strong> Вот здесь самое интересное. Тесты проходили — и проходили честно. Они подавали на вход синтетическое событие типа «пришло сообщение». В таком событии <strong>нет объекта callback вообще</strong>. То есть в тестовой фикстуре первая ветка цепочки была единственной доступной и, следовательно, правильной. Тест проверял ровно тот сценарий, в котором ошибки не существует.</p>

<p><strong>Код-ревью.</strong> Человек на ревью видит защитную цепочку по документированным полям с разумным дефолтом. Здесь нет запаха. Информации, необходимой для обнаружения дефекта, вообще нет в коде — она находится в поведении платформы на одном конкретном типе события. Ни один читатель диффа не обязан её знать.</p>

<h2 id="eshche-dva">Ещё два таких же места в той же функции</h2>

<p>Когда мы нашли первый случай, я перечитал функцию разбора событий целиком — и нашёл ещё два места той же природы.</p>

<p><strong>Первое — членство в канале.</strong> В Telegram членство проверяется запросом: спрашиваешь у API, состоит ли пользователь в канале, получаешь ответ. В MAX это поток событий: <code>user_added</code> и <code>user_removed</code>. И на этих событиях субъект лежит уже не в <code>callback</code> и не в <code>message.sender</code>, а в <code>payload["user"]</code>. То есть <code>sender</code> приходится переопределять третий раз, отдельной веткой:</p>

<pre><code>if mapped in (UpdateType.CHANNEL_SUBSCRIBED, UpdateType.CHANNEL_UNSUBSCRIBED):
    member = payload.get("user") or {}
    sender = member or sender</code></pre>

<p><strong>Второе — сам идентификатор.</strong> Форма объекта пользователя отличается между типами событий, поэтому собирать идентификатор приходится ещё одной цепочкой:</p>

<pre><code>max_user_id = str(
    sender.get("user_id") or sender.get("id") or callback.get("user_id") or ""
)</code></pre>

<p>Итого: <strong>три разных ответа на вопрос «кто пользователь» внутри одной функции, на одной платформе</strong>. Каждый из них правильный в своём контексте и катастрофически неправильный в чужом. Ни один из них не защищён типами, ни один не проверяется тестом, который вы напишете, не зная об этой особенности заранее.</p>

<h2 id="vcard">Третий вариант той же болезни: телефон в vCard</h2>

<p>Ещё один случай, чуть менее драматичный, но идентичный по механике.</p>

<p>Когда пользователь делится номером телефона, Telegram отдаёт номер структурным полем. MAX кладёт его внутрь vCard-строки, и её надо разбирать самому:</p>

<pre><code>if not phone and p.get("vcf_info"):
    m = re.search(r"TEL[^:]*:\\s*([+\\d][\\d\\s()\\-]*)", p["vcf_info"])
    if m:
        phone = m.group(1).strip()</code></pre>

<p>Регулярка написана нормально. Но если платформа пришлёт вариант форматирования, который она не покрывает, <code>phone</code> останется <code>None</code>. А <code>None</code> — это <strong>валидное значение</strong>: оно означает «пользователь не поделился номером». Онбординг для такого человека тихо остановится, и он окажется единственным, у кого не работает регистрация, при полностью здоровом сервисе.</p>

<p>Обратите внимание на общий узор всех трёх случаев. Ни в одном из них система не приходит в некорректное состояние. Во всех трёх она приходит в <em>корректное состояние, соответствующее другому сценарию</em>. Это принципиально другой класс дефектов, чем те, на которые рассчитаны наши инструменты.</p>

<h2 id="klass-bagov">Класс дефектов: когда верный и неверный ответ неотличимы</h2>

<p>Сформулирую общее правило, ради которого я всё это и разбираю.</p>

<div class="blog-callout blog-callout-warning"><i class="ph-fill ph-warning" aria-hidden="true"></i> Опасны не те ошибки, которые падают. Опасны те, где неверный результат и верный результат имеют одинаковую форму, одинаковый поток управления и одинаковые логи.</div>

<p>Инструменты, которыми мы ловим баги, устроены одинаково: они сравнивают поведение системы с описанием известного сбоя. Тест — это описание. Бенчмарк — описание. Правило линтера — описание. Алерт в мониторинге — описание. Автоматический ревьюер — набор описаний, выученных на чужих ошибках.</p>

<p>У дефектов этого класса описания нет до тех пор, пока кто-нибудь на них не наступил. Их нельзя «покрыть тестами» заранее, потому что чтобы написать тест, нужно уже знать, что <code>callback.user</code> должен идти первым. А если вы это знаете, вы просто напишете код правильно.</p>

<p>Отсюда неприятный вывод: увеличение количества юнит-тестов на такой системе почти не двигает вероятность поймать этот класс. Вы наращиваете покрытие сценариев, которые уже придумали, а инциденты приходят из тех, которые не придумал никто.</p>

<h2 id="ii-i-kod">Почему генерация кода делает этот класс массовым</h2>

<p>Здесь я скажу вещь, которая расходится с типичной подачей темы.</p>

<p>Когда обсуждают риски кода, написанного моделью, обычно говорят про галлюцинации, про несуществующие библиотеки, про уязвимости. Это реальные проблемы, но у них есть общее свойство: они <strong>шумные</strong>. Несуществующий импорт падает на первом запуске. Синтаксическая чушь не компилируется. Плохая уязвимость находится сканером.</p>

<p>Настоящая проблема тише. Модель прекрасно генерирует правдоподобный код. Цепочка проверок из моего примера — ровно такой код: аккуратный, защитный, с дефолтом, идиоматичный. Именно так пишет хороший разработчик и именно так пишет хорошая модель.</p>

<p>Чего модель не знает — это поведения конкретной платформы на конкретном типе события. Такого знания нет ни в документации в общем виде, ни в типах, ни в примерах. Оно появляется только у того, кто уже отлаживал этот случай руками.</p>

<p>Дальше арифметика простая. Объём написанного кода растёт, скорость написания растёт, доля кода, который никто не читал построчно, растёт. Класс «громких» ошибок ловится инструментами всё лучше. Класс «тихих» — не ловится вообще, и его доля в общем числе инцидентов растёт просто потому, что знаменатель уменьшается.</p>

<p>Формулируя коротко: <strong>автоматические ревьюеры поднимают планку по тем сбоям, которые кто-то уже сумел назвать. Они не трогают те, которые никто не догадался назвать.</strong> У большинства команд, катающих ботов и агентов, почти все продовые инциденты — из второй категории.</p>

<h2 id="chto-delat">Что делать вместо наращивания тестов</h2>

<p>Четыре приёма, которые реально окупаются на такой системе. Я расставил их в порядке отношения пользы к затратам.</p>

<h3>1. Реальный трафик вместо синтетических фикстур</h3>

<p>Синтетическая фикстура — это ваше представление о том, как выглядит событие. Именно это представление и было неверным. Записывайте настоящие апдейты с боевого вебхука, обезличивайте и складывайте в набор для прогонов. Один настоящий апдейт от нажатия кнопки поймал бы этот баг мгновенно, потому что в нём есть <code>callback</code>.</p>

<p>Практически: заведите режим, в котором сервис пишет сырой payload каждого типа события по одному экземпляру. Через неделю у вас будет каталог реальных форм событий платформы. Это дешевле любого мока и честнее.</p>

<h3>2. Прогон на одинаковом входе и дифф выхода</h3>

<p>Запускайте систему на одном и том же наборе входов до и после изменений и сравнивайте выход целиком, а не «упало / не упало». Тихий дрейф проявляется как разница в диффе и никогда — как исключение.</p>

<p>Это же ловит регрессии при обновлении зависимостей и при изменениях на стороне платформы, о которых вас никто не предупредит. Мессенджеры меняют формат событий без объявления войны.</p>

<h3>3. Проверять эффект, а не отсутствие ошибки</h3>

<p>«Обработчик отработал без исключений» — это не критерий успеха. Критерий успеха — «состояние изменилось так, как должно было». В нашем случае правильная проверка звучит так: после события нажатия кнопки в базе должна появиться запись действия, привязанная к <strong>человеку</strong>. Такая проверка ловит баг сразу, потому что записи не появляется.</p>

<div class="blog-callout blog-callout-info"><i class="ph-fill ph-info" aria-hidden="true"></i> Правило, которое стоит вписать в стандарт разработки: если тест не утверждает ничего про изменение состояния, он не тест, а проверка синтаксиса.</div>

<h3>4. Явный ответ на вопрос «что на шаге, который система не тянет»</h3>

<p>Это самое важное и самое пропускаемое. У любого автоматизированного сценария есть шаг, на котором он не справляется. Вопрос не в том, можно ли этот шаг убрать — нельзя. Вопрос в том, что происходит, когда до него доходит.</p>

<p>В ассистентах записи, которые я делаю для сервисного бизнеса, повторяется одна и та же картина: запросы, на которых бот спотыкается, — это <strong>непропорционально часто самые дорогие клиенты</strong>. Те, кто просит что-то чуть в сторону от стандартного меню. Команда видит процент неуспеха и пытается автоматизировать сильнее. Правильное движение противоположное: раньше распознать уход со сценария и быстрее передать человеку.</p>

<h2 id="dengi">Во что это обходится в деньгах</h2>

<p>Переведу в понятную плоскость, потому что для малого бизнеса это не абстракция.</p>

<p>Тихий сбой стоит дороже громкого по трём причинам.</p>

<p><strong>Время обнаружения.</strong> Громкая ошибка обнаруживается за минуты — вас будит алерт. Тихая живёт до тех пор, пока кто-то не откроет продукт глазами пользователя. В моей практике типичная дистанция — от нескольких часов до нескольких дней. Всё это время продукт формально работает.</p>

<p><strong>Потери не видны в метриках.</strong> Если бот падает, вы видите всплеск ошибок. Если бот тихо не отвечает, вы видите просто более низкую конверсию — и объясняете её сезоном, трафиком, ценой, чем угодно. Причина ищется не там, где находится.</p>

<p><strong>Бьёт по верхушке воронки клиентов.</strong> Смотрите пункт про ассистентов записи: ломается именно нестандартный сценарий, а нестандартный сценарий — это чаще всего клиент с большим чеком.</p>

<p>Отсюда практический вывод для заказчика разработки: <strong>требуйте от подрядчика не «покрытие тестами», а прогон на реальном трафике и проверки на эффект</strong>. Первое звучит солиднее и стоит дешевле в реализации. Второе действительно защищает.</p>

<h2 id="cheklist">Чек-лист приёмки для бота или агента</h2>

<p>Список, по которому я принимаю собственную работу. Годится и для приёмки у подрядчика.</p>

<ul>
  <li>Собран каталог реальных событий платформы: по одному сохранённому сырому payload на каждый тип. Не моки.</li>
  <li>Есть прогон системы на этом каталоге, результат сравнивается диффом с предыдущим прогоном.</li>
  <li>Каждый сценарий проверяется утверждением про изменение состояния, а не про отсутствие исключения.</li>
  <li>Для каждой точки, где определяется «кто пользователь», написано, из какого поля и на каком типе события он берётся. Это документ, а не комментарий в коде.</li>
  <li>Определено поведение на шаге, который система не тянет: кому и как быстро передаётся управление.</li>
  <li>Первые полсотни настоящих взаимодействий прочитаны человеком глазами. Не выборка, не агрегаты — подряд.</li>
  <li>Есть ручной сценарий приёмки: живой человек нажимает каждую кнопку в проде после выката. Да, это скучно. Это единственное, что поймало бы описанный баг за тридцать секунд.</li>
</ul>

<h2 id="perenos">Отдельно про перенос продукта между платформами</h2>

<p>Раз уж речь зашла о двух мессенджерах, зафиксирую вывод, который стоил мне этой отладки.</p>

<p><strong>Перенос бота на вторую платформу — это не портирование, а переписывание всех предположений о том, кто такой пользователь.</strong> Внешне API похожи: там сообщения и здесь сообщения, там кнопки и здесь кнопки, там вебхук и здесь вебхук. Именно это сходство и опасно, потому что оно провоцирует копирование кода с одной платформы на другую.</p>

<p>Расхождения при этом лежат не в очевидных местах вроде формата запроса, а в семантике: кто считается отправителем, где лежит человек, как узнаётся членство в канале, каким образом приходит телефон. Каждое такое расхождение — кандидат в тихий сбой, потому что скопированный код будет синтаксически валидным и логически осмысленным на обеих платформах, а верным — только на одной.</p>

<p>Практический вывод для планирования бюджета: закладывайте на вторую платформу не «десять процентов на адаптацию», а полноценный этап с собственной отладкой на реальных событиях. Экономия здесь возвращается сбоями, которые вы не увидите.</p>

<h2 id="kak-iskat">Как искать такие места у себя, не дожидаясь инцидента</h2>

<p>Есть быстрый приём, который не требует ни новых инструментов, ни переписывания тестов. Займёт полдня на среднем проекте.</p>

<p>Найдите в коде все места, где значение выбирается из нескольких кандидатов подряд: цепочки через <code>or</code>, конструкции вида «взять отсюда, иначе отсюда», разбор чужой полезной нагрузки с несколькими возможными полями. В типичной интеграции таких мест от десяти до сорока.</p>

<p>Для каждого ответьте на два вопроса письменно. Первый: <strong>при каких входных данных срабатывает каждая ветка</strong> — не в теории, а на реальных событиях из вашего каталога. Второй: <strong>что произойдёт, если сработает не та ветка</strong> — упадёт ли что-нибудь, или система просто пойдёт дальше с неверным значением.</p>

<p>Все места, где ответ на второй вопрос звучит как «просто пойдёт дальше», — это ваши кандидаты в тихие сбои. Их обычно немного, единицы. Именно на них имеет смысл потратить проверку на эффект и прогон на реальном трафике, а не размазывать усилия по всему проекту ровным слоем.</p>

<p>Отдельно проверьте границы, где вы принимаете данные от внешней системы: вебхуки, колбэки платёжных шлюзов, ответы чужих API. Внутри собственного кода вы контролируете форму данных. На границе — нет, и меняется она без предупреждения.</p>

<h2 id="faq">Частые вопросы</h2>

<p><strong>Разве нормальный линтер или строгая типизация не поймали бы это?</strong> Нет. С точки зрения типов код корректен: словарь, обращение по существующим ключам, строка на выходе. Проблема не в типе значения, а в том, что значение семантически относится к другому субъекту. Ни один статический анализатор не знает, что автор сообщения с кнопкой — это бот, а не человек.</p>

<p><strong>Почему не помог мониторинг?</strong> Потому что мониторить было нечего. Сервис отвечал 200, ошибок не было, задержки в норме. Единственная метрика, которая показала бы проблему, — количество успешно обработанных нажатий кнопок, то есть метрика на бизнес-эффект, а не на техническое здоровье. Такие метрики почти никто не заводит заранее.</p>

<p><strong>Это специфика MAX или общая проблема?</strong> Конкретный случай специфичен для MAX, но класс дефектов универсален. Любая интеграция, где вы разбираете чужую полезную нагрузку и выбираете поле из нескольких кандидатов, — это то же самое место. Платёжные шлюзы, CRM, вебхуки маркетплейсов, ЭДО: везде, где есть цепочка «взять отсюда, иначе отсюда».</p>

<p><strong>Как быстро это чинится, если найдено?</strong> Первый случай — одна переставленная строка, минуты. Полный аудит функции разбора событий и добавление прогона на реальных событиях — от одного до трёх рабочих дней в зависимости от количества типов событий. Несопоставимо дешевле, чем неделя работы продукта, который формально работает.</p>

<p><strong>Значит, код от нейросетей писать нельзя?</strong> Можно и нужно, я сам так работаю каждый день. Вопрос не в том, кто написал строку, а в том, чем вы проверяете результат. Для внутренних инструментов, которыми пользуются сотрудники, планка ниже: сотрудник сам скажет, что сломалось. Для того, что видит клиент, планка выше — клиент не заводит баг-репорт, он просто уходит.</p>

<h2 id="vyvody">Что забрать с собой</h2>

<p>Три мысли, ради которых стоило писать этот текст.</p>

<p>Первая: <strong>самые дорогие ошибки не падают</strong>. Они возвращают неверный ответ в правильной форме, и все ваши инструменты сообщают, что всё хорошо.</p>

<p>Вторая: <strong>тесты защищают от того, что вы уже придумали</strong>. В моём случае тест был написан аккуратно и проходил честно — просто он описывал сценарий, в котором ошибки не существует. Количество таких тестов можно наращивать бесконечно без всякого эффекта на этот класс дефектов.</p>

<p>Третья, и она важнее двух предыдущих. Чем лучше машины справляются с задачами, которые мы умеем формулировать, тем сильнее работа разработчика сдвигается к формулированию того, чего ещё никто не сформулировал. Найти, назвать и описать сбой, о котором нет ни строчки ни в одной документации, — это <strong>наименее автоматизируемая часть работы, а не наиболее</strong>.</p>

<p>Если вы заказываете бота, агента или интеграцию — спрашивайте у подрядчика не про покрытие тестами, а про то, на каких событиях он прогонял систему и что именно утверждают его проверки. Ответ на этот вопрос говорит о качестве будущего продукта больше, чем любое портфолио.</p>
`;
